<section id="circles" class="container">
    <h1>All Gigs</h1>
    {{#each c}}


    <div class="circles">
        <h3>Theme: {{theme}}</h3>
        <p>Description: {{description}}</p>
        <p>Initiator: {{initiatorid}}</p>
        {{!-- <ul>
        <li>Budget: $10,000</li>
        <li><a href="mailto:{{contactemail}}" class="btn btn-reverse">Apply Now</a></li>
      </ul>
      <div class="tech">
        <small>Technologies Needed: <span>react, redux, javascript</span></small>
      </div> --}}
    </div>

    {{else}}
    <p>No circles in db</p>
    {{/each}}
</section>